import { RecommendedFilterPipe } from './recommended-filter.pipe';

describe('RecommendedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecommendedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
