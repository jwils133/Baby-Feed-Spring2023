import { ResultRoundPipe } from './result-round.pipe';

describe('ResultRoundPipe', () => {
  it('create an instance', () => {
    const pipe = new ResultRoundPipe();
    expect(pipe).toBeTruthy();
  });
});
