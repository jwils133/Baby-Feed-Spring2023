import { TrackerFilterPipe } from './tracker-filter.pipe';

describe('TrackerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
