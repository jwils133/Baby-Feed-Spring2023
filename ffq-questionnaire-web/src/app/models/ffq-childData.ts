//Classed to store admin user data in components

export class FFQChildData {
  weight: string;
  height: string;
  age: string;

  constructor(weight: string, height: string, age: string) {
    this.weight = weight;
    this.height = height;
    this.age = age;
  }
}
